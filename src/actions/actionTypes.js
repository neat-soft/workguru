import { createTypes } from 'reduxsauce';

export default createTypes(`
    LOGIN
    HOME
    USER_PROFILE
    SETTING
    EDIT_INFO
    PROFILE_PREVIEW
    MESSAGE
    CHAT
    BACK
    LOGOUT
`);